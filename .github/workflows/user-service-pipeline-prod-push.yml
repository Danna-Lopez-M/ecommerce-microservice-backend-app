name: Prod, on PUSH Java CI/CD of user-service-ecommerce-boot, ecommerce-microservice-backend app

on:
  push:
    branches: [ master ]
    paths:
      - 'user-service/**'
      - 'pom.xml'
      - '.github/workflows/user-service-pipeline-prod-push.yml'
      - '.github/workflows/reusable-pr-pipeline.yml'
      - '.github/workflows/reusable-service-pipeline.yml'

jobs:
  pipeline:
    uses: ./.github/workflows/reusable-service-pipeline.yml
    with:
      service_name: user-service
      service_path: user-service
      dockerfile_path: user-service/Dockerfile
      environment: prod
      docker_image_name: user-service-ecommerce-boot
      skip_deploy: false
    secrets: inherit
