name: Prod, on PUSH Java CI/CD of favourite-service-ecommerce-boot, ecommerce-microservice-backend app

on:
  push:
    branches: [ master ]
    paths:
      - 'favourite-service/**'
      - 'pom.xml'
      - '.github/workflows/favourite-service-pipeline-prod-push.yml'

jobs:
  pipeline:
    uses: ./.github/workflows/reusable-service-pipeline.yml
    with:
      service_name: favourite-service
      service_path: favourite-service
      dockerfile_path: favourite-service/Dockerfile
      environment: prod
      docker_image_name: favourite-service-ecommerce-boot
      skip_deploy: false
    secrets: inherit
