name: Prod, on PR Java CI/CD of cloud-config, ecommerce-microservice-backend app

on:
  pull_request:
    branches: [ master ]
    paths:
      - 'cloud-config/**'
      - 'pom.xml'
      - '.github/workflows/cloud-config-pipeline-prod-pr.yml'

jobs:
  pipeline:
    uses: ./.github/workflows/reusable-pr-pipeline.yml
    with:
      service_name: cloud-config
      service_path: cloud-config
    secrets: inherit
