name: Prod, on PUSH Java CI/CD of product-service-ecommerce-boot, ecommerce-microservice-backend app

on:
  push:
    branches: [ master ]
    paths:
      - 'product-service/**'
      - 'pom.xml'
      - '.github/workflows/product-service-pipeline-prod-push.yml'
      - '.github/workflows/reusable-pr-pipeline.yml'
      - '.github/workflows/reusable-service-pipeline.yml'

jobs:
  pipeline:
    uses: ./.github/workflows/reusable-service-pipeline.yml
    with:
      service_name: product-service
      service_path: product-service
      dockerfile_path: product-service/Dockerfile
      environment: prod
      docker_image_name: product-service-ecommerce-boot
      skip_deploy: false
    secrets: inherit
